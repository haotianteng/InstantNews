# JOJO v2 Progress Log
Started: Fri 17 Apr 2026 07:45:40 AM EDT
PRD: Company Information Infrastructure Upgrade — Normalize company data into 6 purpose-built Postgres tables (companies, company_financials, company_fundamentals + history SCD-2, company_competitors, institutional_holders, insider_transactions). Add Redis (ElastiCache) cache-aside via BaseRepository pattern. Aggregate via CompanyService.get_full_profile() with ThreadPoolExecutor parallel fan-out and on-demand backfill guarded by per-(ticker, domain) Redis mutex. Expose unified GET /api/company/<ticker>/profile endpoint. Refactor existing /api/market/<symbol>/* routes to use new repos (backward compatible). Backfill from existing company_data_cache JSON blobs, then deprecate. Add scheduled EDGAR (10-Q/K, 13F, Form 4) and Polygon fundamentals ingestion via APScheduler with window-aware 13F polling and monthly SEC calendar probe. Seed S&P 500 core tickers up front (rate-gated) with tighter refresh cadence; long-tail tickers grow organically via on-demand backfill. Stay synchronous Flask + SQLAlchemy 2.0 with clean repo interfaces designed for a future mechanical async migration. Source PRD: tasks/prd-company-info-infra.md
Mode: Adversarial (loop)
---

## Codebase Patterns

- **Cache singleton pattern** (`app/cache/redis_client.py`): lazy thread-safe `get_redis()` that resolves URL from `Config.REDIS_URL` with env-var fallback. Uses `ConnectionPool.from_url(max_connections=50, decode_responses=False)`. Future repos should import `from app.cache import get_redis` and serialize payloads explicitly (JSON/msgpack), not rely on `decode_responses`.
- **Health endpoint pattern** (`app/routes/health.py`): separate blueprint for cheap anonymous probes; exceptions from Redis/DB checks are caught and surfaced in the JSON body, not re-raised. Returns 200/503 based on overall status. `/api/stats` remains the ECS container healthcheck target — don't repoint it until a story says so.
- **CDK security-group ordering**: for resources like ElastiCache that need SG ingress from ECS services created later in the stack, define the resource's own SG up front (outbound disabled), then call `sg.add_ingress_rule(peer=service.connections.security_groups[0], ...)` after each ECS service is constructed. Avoids circular construct ordering that would happen with `allow_from`.
- **Prod vs dev compose**: `docker-compose.yml` is the canonical service graph; `docker-compose.override.yml` is the dev-only layer (SQLite, exposed ports, profile-disabled prod-only services). Add `depends_on.<svc>.condition: service_healthy` on the app service so Flask waits for deps to be healthy before starting.
- **Alembic dialect gate for Postgres-specific DDL** (`migrations/versions/014_add_companies_table.py`): when a migration needs a Postgres-only feature (partial index, `JSONB`, `ARRAY`, tsvector, etc.) but the project also runs on SQLite locally, branch on `op.get_bind().dialect.name == "postgresql"` and emit a portable fallback in the `else`. Example: `op.create_index(..., postgresql_where=sa.text("..."))` in Postgres, plain `op.create_index(name, table, [col])` on SQLite. Keeps one migration file + makes local test bootstraps survive.
- **ORM vs Pydantic namespace split** (`app/models/`): `app/models/__init__.py` holds SQLAlchemy ORM classes (legacy compatibility — all existing `from app.models import User` imports resolve here). Domain-specific Pydantic schemas live as sibling modules `app/models/<domain>.py` (e.g. `app/models/company.py`) and are imported explicitly as `from app.models.company import Company`. Do NOT re-export Pydantic classes from `__init__.py` — keeps the two namespaces separate so ORM `Company` and Pydantic `Company` can share a name without shadowing.
- **Package conversion preserving history**: when converting `pkg.py` → `pkg/__init__.py`, use `git mv pkg.py pkg/__init__.py` (after `mkdir -p pkg`). Git records it as a rename, history follows.
- **SCD-2 via Postgres trigger** (`migrations/versions/016_add_company_fundamentals.py`): current-view table + append-only history table with a BEFORE UPDATE trigger that copies OLD row into history and updates NEW.updated_at := NOW(). Trigger + underlying PL/pgSQL function share the same name (spec convention — the pg_trigger assertion keys on `tgname`). Downgrade order is load-bearing: drop trigger → drop function → drop tables (Postgres refuses to drop a function whose triggers still exist). Dialect-gated — SQLite cannot express this cleanly, so the corresponding repo writes history rows manually on non-Postgres dialects. Trigger body uses `CREATE OR REPLACE FUNCTION` so re-applying the migration after a downgrade works without errors.
- **DESC-order indexes via sa.text()** (migrations 015–019): Alembic's `op.create_index(name, table, [col])` cannot express the DESC modifier directly, but passing `[sa.text("ticker"), sa.text("period_end DESC")]` works dialect-agnostically — Postgres honors DESC, SQLite tolerates it. Use this pattern for every index that supports "latest N" read paths (financials, fundamentals_history, institutional_holders, insider_transactions).
- **Dedup by PK vs UNIQUE vs composite UNIQUE** (Layer 2+3 tables): three patterns in use. (a) **Composite PK dedup** — `company_financials` uses PK (ticker, period_end, period_type) so re-ingesting the same filing fails with a PK violation, no app logic needed. (b) **UNIQUE dedup with surrogate BIGSERIAL id** — `institutional_holders` keeps id as the natural monotonic PK for event ordering and overlays a UNIQUE on (ticker, institution_cik, report_date) as the dedup gate. (c) **Idempotent-ingest UNIQUE beyond spec** — `insider_transactions` (US-007) uses a surrogate BIGSERIAL PK plus a composite UNIQUE on (ticker, insider_name, transaction_date, transaction_type, shares, form_type) that deliberately EXCLUDES price_per_share and total_value. Rationale: SEC and redistributors publish prices to slightly different precision, so including price in the key would duplicate the same Form 4 when ingested from multiple sources. Pick the strictest pattern that lets re-ingest be a no-op from all plausible upstream paths.
- **FK ON DELETE RESTRICT for reference tables** (every Layer 2/3 table → companies): spec mandates RESTRICT (not CASCADE) so master-row deletion is explicitly blocked while child rows exist. Companies that delist should set `delisted_at` instead of being deleted — the Alembic migrations enforce this with `sa.ForeignKeyConstraint([...], [...], ondelete='RESTRICT')`. Declare ORM-side `ForeignKeyConstraint` in `__table_args__` rather than inline `ForeignKey()` on the Column so the RESTRICT is visible at the table level.
- **BigInteger autoincrement → BIGSERIAL** (migrations 018/019): `sa.Column('id', sa.BigInteger(), primary_key=True, autoincrement=True)` emits `BIGSERIAL` on Postgres (shows as `bigint DEFAULT nextval(...)` in `\\d`) and `INTEGER AUTOINCREMENT` on SQLite. Portable across dialects with no additional code.

---

## 2026-04-17 - US-001 - IMPLEMENTOR - Redis infra landed
- Added `redis:7-alpine` service to `docker-compose.yml` (+ `redis-data` volume, healthcheck, appendonly persistence) and `docker-compose.override.yml` publishes 6379 to host.
- Added `REDIS_URL` to `Config` (default `redis://localhost:6379/0`) and wired it through docker-compose env + CDK shared_env for web/admin/worker ECS tasks.
- New `app/cache/` package: singleton `get_redis()` with `max_connections=50` pool; thread-safe, lazy.
- New `/health` blueprint reports Redis ping + `SELECT 1` DB check; 200 when both OK, 503 otherwise.
- CDK stack: `aws_elasticache.CfnCacheCluster` (engine redis 7.1, `cache.t4g.micro`, 1 node, port 6379) in PRIVATE_WITH_EGRESS subnets; dedicated `redis_sg` with inbound 6379 only from ECS service SGs (web/admin/worker). Added `RedisEndpoint` + `RedisUrl` CfnOutputs.
- `requirements.txt` pins `redis>=5.0.0`.
- Files changed: docker-compose.yml, docker-compose.override.yml, app/config.py, requirements.txt, app/cache/__init__.py, app/cache/redis_client.py, app/routes/health.py, app/routes/__init__.py, infra/stack.py.
- Self-validation: AST parse OK on all 4 Python files; `docker compose config` valid; `docker compose -f docker-compose.yml up -d redis` + `redis-cli PING` returns PONG; `npx cdk synth` emits `AWS::ElastiCache::CacheCluster` (found once) and REDIS_URL in 3 task definitions; `mypy --follow-imports=silent app/cache/` exits 0; Flask test client hits `/health` → 200 with `{redis:'ok', db:'ok', status:'ok'}`, hits error path with bad REDIS_URL → 503 with specific redis error.
- **Learnings / Environment notes for Tester:**
  - Host machine already runs a system redis on 127.0.0.1:6379 → `docker compose up -d redis` (dev-override path) will fail to bind the host port. Either stop system redis (`sudo systemctl stop redis-server`) or use `docker compose -f docker-compose.yml up -d redis` which omits the override's host port mapping.
  - mypy install command: `/home/haotiant/conda/bin/pip install mypy types-redis`. Running `mypy app/cache/` (without `--follow-imports=silent`) surfaces pre-existing errors in `app/services/cache_manager.py` and `app/services/edgar_client.py` — these are NOT from this story. Use `mypy --follow-imports=silent app/cache/` for a scoped pass.
  - `app/cache/redis_client.py` provides `reset_redis_client()` — tests that swap `REDIS_URL` between cases must call it to drop the cached singleton.
  - `/health` is a NEW route; ECS healthcheck still targets `/api/stats` — intentional, changing that is out of scope.
  - CDK Redis endpoint is a CloudFormation token that resolves at deploy time (`f"redis://{cluster.attr_redis_endpoint_address}:{cluster.attr_redis_endpoint_port}/0"`); the synthesized template contains `Fn::GetAtt` refs that read correctly.
---

## 2026-04-17 - US-001 - TESTER - TEST_PASSED (attempt 1)
- Ran all 6 test_assertions against commit 3484c54.
- Commands executed (captured under jojo/test_results/US-001/attempt-1/):
  - `docker compose -f docker-compose.yml up -d redis` + `docker compose -f docker-compose.yml exec -T redis redis-cli PING` → PONG. Used prod compose to dodge host-port conflict with system redis (implementor's documented workaround).
  - `REDIS_URL=redis://127.0.0.1:6379/0 python -c "from app.cache.redis_client import get_redis; r=get_redis(); r.set('test','1'); assert r.get('test')==b'1'"` → exit 0.
  - `grep -E '^redis>=' requirements.txt` → matched `redis>=5.0.0`, exit 0.
  - `mypy --follow-imports=silent app/cache/` → Success: no issues found in 2 source files. (Unscoped `mypy app/cache/` exits 1 solely because of pre-existing errors in app/services/{cache_manager,edgar_client,market_data}.py — not US-001 code. Implementor flagged this in handoff.)
  - `cd infra && cdk synth` exit 0; template contains 1 `AWS::ElastiCache::CacheCluster` + CfnSubnetGroup + 3 `REDIS_URL` env injections (web/admin/worker).
  - Flask test client `GET /health` with live redis → HTTP 200, body `{'db':'ok','redis':'ok','status':'ok'}`.
- Verdict: PASS. Set status=TEST_PASSED, passes=true. Wrote test_report and test_evidence in prd.json.
- Stopped the docker redis container after verification so host redis remains the only listener.
- **Learnings:**
  - When a project has pre-existing mypy errors in transitively-imported modules, strict `mypy <newdir>` will exit non-zero even if `<newdir>` itself is clean. For future stories, test assertion `mypy <path>` should be interpreted as "no NEW errors in <path>'s own source files". `--follow-imports=silent` is the scoped equivalent and matches the spirit of the assertion.
  - `docker compose -f docker-compose.yml` (canonical file only, no override) is the right incantation on this host — the override's `6379:6379` host-port publish collides with the always-on system redis. Downstream stories that depend on Redis should follow the same pattern.
  - Flask test client + sqlite in-memory DB is a fast way to validate `/health`-style endpoints without starting a real server; Firebase Admin init runs but otherwise is harmless (only fails on Google calls, which `/health` doesn't make).
---

## 2026-04-17 - US-002 - IMPLEMENTOR - companies master table landed
- Added Alembic revision `014_add_companies_table.py` creating `companies` (14 spec columns + `delisted_at TIMESTAMP NULL`), PK on `ticker`, UNIQUE on `cik`, indexes on sector/industry, partial `idx_companies_active ON companies(ticker) WHERE delisted_at IS NULL` (Postgres) with plain-index fallback on SQLite.
- Converted `app/models.py` → `app/models/__init__.py` via `git mv` to host both ORM (legacy + new `Company` class) and Pydantic sibling modules. All existing `from app.models import User, News, Subscription, ...` imports continue to resolve.
- Added SQLAlchemy ORM `Company` in `app/models/__init__.py` — Date ipo_date, DateTime timestamps with `server_default=func.now()`, Boolean `is_active` default TRUE, `delisted_at` nullable DateTime.
- Added Pydantic v2 `Company` in `app/models/company.py` — `from_attributes=True` for future `Company.model_validate(orm_row)` in repositories.
- Updated `migrations/env.py` to import `Company` so `Base.metadata` sees the new table.
- Files changed: migrations/versions/014_add_companies_table.py, app/models/__init__.py (renamed from app/models.py), app/models/company.py, migrations/env.py.
- Self-validation: orm import smoke (all 10 models + Company), pydantic import + instantiation smoke, ran Postgres 16 on localhost:5433 (avoids host system-postgres on 5432), `alembic upgrade head` through 014 ran cleanly, `\d companies` shows 17 cols + all 5 indexes incl. partial, `INSERT INTO companies(ticker,name) VALUES('AAPL','Apple Inc.')` populates server defaults, `alembic downgrade -1` + `\d companies` → relation missing, round-trip `alembic upgrade head` restores 014. `mypy --follow-imports=silent app/models/company.py` → clean.
- **Learnings / Environment notes for Tester:**
  - Host runs system Postgres on 127.0.0.1:5432. `docker compose --profile prod up -d postgres` brings up `signal-postgres` but does NOT publish a host port (canonical `docker-compose.yml` lacks `ports:` for postgres), so `alembic ... localhost:5432` silently hits system postgres and auth-fails. Tester should run a one-off container with an explicit port: `docker run -d --name signal-postgres-test -p 5433:5432 -e POSTGRES_USER=signal -e POSTGRES_PASSWORD=signal -e POSTGRES_DB=signal_news postgres:16-alpine`, wait for `docker exec signal-postgres-test pg_isready -U signal`, then `DATABASE_URL=postgresql://signal:signal@localhost:5433/signal_news alembic upgrade head`. Or `docker exec signal-postgres-test psql -U signal -d signal_news -c '\d companies'`.
  - mypy: use `mypy --follow-imports=silent app/models/company.py` — same US-001 precedent for dodging pre-existing errors in app/services/*.
  - Pydantic v2 schema uses `model_config = {"from_attributes": True}`; tester should use `PydCompany(...)` instantiation, not `parse_obj` (v1 API).
  - Git shows `RM app/models.py -> app/models/__init__.py` — that's expected; same file, moved into a package.
  - After `alembic downgrade -1`, re-run `alembic upgrade head` to restore the table before handing the DB off to downstream stories (US-003 has FK to companies).
---

## 2026-04-17 - US-002 - TESTER - TEST_PASSED (attempt 1)
- Ran all 6 test_assertions against commit cecca8b via `jojo/test_results/US-002/attempt-1/validate.sh`.
- Environment: one-off `signal-postgres-test` container (`postgres:16-alpine`) published on host 5433 → container 5432, `DATABASE_URL=postgresql://signal:signal@localhost:5433/signal_news`, python at `/home/haotiant/conda/bin/python`, alembic at `/home/haotiant/conda/bin/alembic`, mypy at `/home/haotiant/conda/bin/mypy`. Tore container down at end.
- Results per assertion (artifacts under `jojo/test_results/US-002/attempt-1/`):
  1. **upgrade + schema** — `alembic upgrade head` ran 001→014 cleanly (upgrade.log final line: "Running upgrade 013 -> 014, Add companies master reference table"). `\d companies` (companies_schema.log) shows all 14 spec columns (ticker, cik, name, exchange, sector, industry, country, currency, description, website, employee_count, founded_year, ipo_date, is_active) + created_at + updated_at + delisted_at = 17 total; `delisted_at` is `timestamp without time zone` and nullable; indexes block lists pk_companies, uq_companies_cik, idx_companies_sector, idx_companies_industry, and `idx_companies_active btree (ticker) WHERE delisted_at IS NULL` (partial index per spec). PASS.
  2. **indexes** — indexes.log: `pk_companies`, `uq_companies_cik`, `idx_companies_sector`, `idx_companies_industry`, `idx_companies_active` (5 rows). All three required names present. PASS.
  3. **ORM import** — orm_import.log: stdout `AAPL` (exit 0). Leading `POLYGON_API_KEY not set — market data service disabled` is a stderr warning from app bootstrap, not an error. PASS.
  4. **Pydantic import** — pyd_import.log: stdout `AAPL` (exit 0), including `delisted_at=None` kwarg. PASS.
  5. **downgrade** — downgrade.log ends cleanly at "Running downgrade 014 -> 013" with no errors; post_downgrade.log contains `Did not find any relation named "companies".` confirming the table was dropped. PASS.
  6. **mypy** — mypy.log: `Success: no issues found in 1 source file` (scoped with `--follow-imports=silent` per US-001 precedent). PASS.
- Verdict: PASS. Set status=TEST_PASSED, passes=true, fail_count stays at 0. Wrote test_report and test_evidence in prd.json.
- **Learnings:**
  - **Postgres port-publishing gotcha (preserved for future migration testers):** this host has a system Postgres listening on 127.0.0.1:5432 that does NOT have the `signal` role. The project's `docker-compose.yml` declares a `postgres` service without a `ports:` mapping (intentional for prod-style), so `docker compose --profile prod up -d postgres` silently starts a container whose 5432 is not reachable from the host — and `alembic ... postgresql://signal:signal@localhost:5432/signal_news` hits the system Postgres and fails auth with a confusing `password authentication failed for user signal`. Always run a dedicated one-off `docker run -d --name signal-postgres-test -p 5433:5432 -e POSTGRES_USER=signal -e POSTGRES_PASSWORD=signal -e POSTGRES_DB=signal_news postgres:16-alpine` for migration tests and point DATABASE_URL at :5433. Tear it down at the end.
  - `docker exec signal-postgres-test psql -U signal -d signal_news -c '\d <table>'` is the clean way to introspect schema without fighting host-vs-container credentials.
  - The Postgres `\d <table>` command returns exit code 0 AND writes `Did not find any relation...` to stderr when the relation is missing. Using `|| echo "EXPECTED ERROR"` in the validate script is a safe idempotent belt-and-braces — stderr already contains the expected text regardless.
  - `set -e` in bash is load-bearing for validation scripts: any failed assertion aborts the run before container teardown. Always clean up leftover containers (`docker rm -f signal-postgres-test 2>/dev/null || true`) at the top of the script.
---

## 2026-04-17 - US-003..US-007 - IMPLEMENTOR - Layer 2 + Layer 3 tables landed
- Shipped 5 Alembic migrations (015 financials, 016 fundamentals+history+SCD-2 trigger, 017 competitors, 018 institutional_holders, 019 insider_transactions) plus 5 Pydantic modules (app/models/{financials,fundamentals,competitors,institutions,insiders}.py) and 6 new ORM classes in app/models/__init__.py (CompanyFinancials, CompanyFundamentals, CompanyFundamentalsHistory, CompanyCompetitor, InstitutionalHolder, InsiderTransaction).
- Commit chain on `jojo/company-info-infra`: fe3b936 (US-003) → bf0427f (US-004) → 73af0d2 (US-005) → 29ff193 (US-006) → (US-007 commit pending after this progress append).
- Self-validation: one-off Postgres 16 container on :5433 (per US-002 pattern), `alembic upgrade head` runs 001→019 cleanly; schema spot-checks match spec for every table; SCD-2 trigger smoke (INSERT pe_ratio=30 → UPDATE pe_ratio=32) produces 1 history row with pe_ratio=30 and valid_from<valid_to, current row shows pe_ratio=32; constraint-rejection smokes pass (duplicate PK on financials, self-reference CHECK on competitors, UNIQUE dedup on institutional_holders + insider_transactions); round-trip `alembic downgrade -1` five times back to 014, then `upgrade head` reapplies cleanly and the trigger is re-created. `mypy --follow-imports=silent app/models/{financials,fundamentals,competitors,institutions,insiders}.py` → Success: no issues found in 5 source files.
- **Learnings / Environment notes for Tester:**
  - **SCD-2 trigger pattern** — Postgres PL/pgSQL trigger + function share the same name (`fn_snapshot_fundamentals_before_update`) per spec. Downgrade order matters: DROP TRIGGER → DROP FUNCTION → DROP tables, otherwise Postgres refuses to drop the function. Dialect-gated — SQLite skips the trigger entirely; downstream repo code will handle SCD-2 manually on non-Postgres.
  - **Composite PK vs BIGSERIAL+UNIQUE tradeoff** — financials uses composite PK (natural key, PK violation is the dedup gate); holders/insiders use BIGSERIAL id PK + UNIQUE constraint (keep surrogate id as monotonic event PK, UNIQUE guards against re-ingest). For Form 4, the UNIQUE key deliberately excludes price_per_share to tolerate precision differences between SEC and redistributors (same filing, slightly different price → still a no-op re-ingest). See Codebase Patterns above.
  - **DESC-order indexes** — use `sa.text('col DESC')` inside `op.create_index(..., [sa.text('ticker'), sa.text('period_end DESC')])`. Postgres honors DESC, SQLite ignores but doesn't error. This pattern shows up in all 4 "latest-N" read paths across these migrations.
  - **One-off Postgres container** — same pattern as US-002: `docker run -d --name signal-postgres-impl -p 5433:5432 -e POSTGRES_USER=signal -e POSTGRES_PASSWORD=signal -e POSTGRES_DB=signal_news postgres:16-alpine`. Wait with `docker exec signal-postgres-impl pg_isready -U signal`. Host system-postgres on :5432 lacks the `signal` role, so :5433 is mandatory.
  - **Tester one-liners** — after `DATABASE_URL=postgresql://signal:signal@localhost:5433/signal_news alembic upgrade head`:
    - `\d company_financials`, `\d company_fundamentals`, `\d company_fundamentals_history`, `\d company_competitors`, `\d institutional_holders`, `\d insider_transactions` to confirm columns + indexes + FKs + checks.
    - `SELECT tgname FROM pg_trigger WHERE tgname='fn_snapshot_fundamentals_before_update'` → 1 row.
    - SCD-2 end-to-end: INSERT companies+fundamentals, sleep 1s, UPDATE fundamentals → verify history row with prior value + valid_from<valid_to.
    - Constraint rejections: second duplicate INSERT on each of the 5 tables' dedup keys raises the expected violation.
    - `alembic downgrade -1` × 5 unwinds back to 014; `alembic upgrade head` reapplies cleanly.
    - `mypy --follow-imports=silent app/models/financials.py app/models/fundamentals.py app/models/competitors.py app/models/institutions.py app/models/insiders.py` → no issues.
  - **Pydantic v2 date coercion** — ISO date strings like '2025-12-31' coerce to `date` automatically; `'2025-12-31'` as kwarg into `Financials(period_end='2025-12-31')` works without manual parsing. For `datetime` fields, pass a `datetime` (or an ISO timestamp string) — `date` strings won't auto-coerce to datetime.
  - **CompanyFinancials ORM in `__init__.py`** — imported additional SA types (BigInteger, CheckConstraint, ForeignKeyConstraint, Numeric, PrimaryKeyConstraint) at the module top; these joined the existing import block. `migrations/env.py` now imports all 6 new ORM classes for metadata registration. Future stories that touch `__init__.py` should keep the import block alphabetized.
---
